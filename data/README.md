Place the assignment's canonical `candidate_dataset.json` here.

The application also accepts an uploaded JSON file with the same structure.
Do not replace the canonical dataset with invented records.
